# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Database
*.db

# Local backup archives (from scripts/backup.sh --dry-run / --local-only)
pjtracker-*.tar.gz

# Saved NF PDFs
pdfs/
images/
.token
# Streamlit / app temp PDFs for in-browser viewing (see streamlit_pdf_link)
src/pjtracker/static/

# SvelteKit (also see frontend/.gitignore)
frontend/node_modules/
frontend/.svelte-kit/
data/
