# pjtracker FastAPI base path (no trailing slash).
# Leave empty to use same-origin `/api/v1` (recommended with `npm run dev` + Vite proxy in `vite.config.ts`).
# For direct calls to FastAPI without proxy: http://127.0.0.1:8000/api/v1
PUBLIC_API_BASE_URL=
